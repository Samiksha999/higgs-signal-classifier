name: Run Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run tests
        run: |
          pip install pytest pandas numpy scikit-learn xgboost shap fastapi uvicorn httpx
          pytest test_api.py -v
